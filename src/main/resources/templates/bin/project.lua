-- properties
Name = "template"
Props = {
    std = "c17",
    version = "0.1",
    type = "bin",
    compiler = "gcc",
    tests = false,
}

-- C files that should not be compiled manually (don't have a header)

-- external dependenciess
Dependencies = {
    -- { "dependency_name", 0.1 }
}
